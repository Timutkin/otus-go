apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "calendar-scheduler.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: scheduler
  template:
    metadata:
      labels:
        app: scheduler
    spec:
      containers:
      - name: scheduler
        image: {{ .Values.image.registry }}{{ .Values.image.repository }}:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: CONFIG_FILE
          value: /etc/calendar/config.yaml
        volumeMounts:
        - name: config
          mountPath: /etc/calendar
      volumes:
      - name: config
        configMap:
          name: {{ include "calendar-scheduler.fullname" . }}-config